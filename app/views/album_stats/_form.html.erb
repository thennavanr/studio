<%= form_for(@album_stat) do |f| %>
  <% if @album_stat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@album_stat.errors.count, "error") %> prohibited this album_stat from being saved:</h2>

      <ul>
      <% @album_stat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
